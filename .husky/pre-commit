#!/bin/sh
# Use nvm node (v22) to match the compiled better-sqlite3 native module
export PATH="/Users/bumworld/.nvm/versions/node/v22.19.0/bin:$PATH"

# Run prebuild first to generate src/content/docs/
USE_SAMPLE_DIR=true npm run prebuild

# Run tests
USE_SAMPLE_DIR=true npm test
