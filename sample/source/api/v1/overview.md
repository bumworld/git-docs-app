# API v1 Overview

The v1 API provides basic CRUD operations for wiki management.

## Base URL

```
https://your-domain.com/api/v1
```

## Authentication

All endpoints require a valid session cookie obtained via Google OAuth.

## Rate Limits

- 100 requests per minute per user
- 1000 requests per hour per user
